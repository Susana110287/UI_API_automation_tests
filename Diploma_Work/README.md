# Проект: Автоматизированное тестирование API и UI Кинопоиска
## Описание проекта
Проект содержит комплексные автоматизированные тесты для проверки функциональности веб‑сервиса «Кинопоиск»:

- UI‑тесты (в test_ui.py) — проверка веб‑интерфейса расширенного поиска;

- API‑тесты (в test_api.py) — валидация REST‑эндпоинтов сервиса.

## Основные возможности:

## UI‑часть:

- поиск фильмов по названию, году, стране, жанрам;
- поиск актёров с верификацией результатов по двум критериям (заголовок страницы и строка поиска).

## API‑часть:

- проверка статусов ответов;
- валидация JSON‑схем;
- тестирование методов GET/POST;
- работа с токенами.

## Используемые технологии:

- Python 3.8+;
- pytest (фреймворк для тестирования);
- Selenium WebDriver (UI‑тесты);
- requests (API‑тесты);
- Allure (генерация отчётов);
- webdriver‑manager (автоустановка драйверов).

## Инструкции по запуску тестов
1. Предварительные требования
Убедитесь, что установлены:

- Python 3.8+;
- pip;
- Google Chrome.

2. Установка зависимостей
Создайте виртуальное окружение:

bash
python -m venv venv
venv\Scripts\activate

Установите зависимости:

bash
pip install -r requirements.txt
Содержание requirements.txt:

- selenium
- pytest
- allure-pytest
- webdriver-manager
- requests

3. Запуск тестов
Базовый запуск всех тестов

## UI‑тесты:
bash
pytest ui/test_ui.py -v

## API‑тесты:
bash
pytest api/test_api.py -v

## Все тесты сразу:
bash
pytest -v

## Запуск с генерацией отчёта Allure
Выполните тесты с сохранением результатов:
bash
pytest ui/test_ui.py api/test_api.py --alluredir=./allure-results

## Запустите отчёт:
bash
allure serve ./allure-results
Отчёт откроется в браузере.

## Запуск отдельных тестов

По метке (например, только UI‑тесты):
bash
pytest -m "ui" -v

## По имени теста:
bash
pytest ui/test_ui.py::TestExtendedSearch::test_search_by_actor -v
pytest api/test_api.py::test_movie_search -v

4. Ожидаемые результаты
Успех: все тесты отмечены как PASSED, отчёт Allure содержит детализацию шагов.

Ошибки: в выводе pytest указаны:
номер шага;
сообщение AssertionError с описанием проблемы.

5. Очистка после тестов

## Удалить отчёт Allure:
bash
rm -rf allure-results/

## Деактивировать виртуальное окружение:
bash
deactivate

## Документация по ручному тестированию
Полная документация по ручному тестированию доступна по ссылке:
👉 https://sky-pro.yonote.ru/share/db34a495-df5f-42a2-a3da-0beca7903748
